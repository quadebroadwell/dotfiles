;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/ubuntu/.emacs.d/emacs_python/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 215)
	(left . 1155)
	(width . 80)
	(height . 34)))

